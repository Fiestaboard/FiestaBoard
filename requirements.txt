# Core dependencies
requests>=2.32.0
pyyaml>=6.0.2
python-dotenv>=1.0.1
schedule>=1.2.2
pytz>=2024.2

# API Server dependencies
fastapi>=0.125.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
httpx>=0.28.0

# Stock market data
yfinance>=0.2.36
finnhub-python>=2.4.19

# Sun position calculations
astral>=3.2

# Docker management (for container restart/upgrade from within container)
docker>=7.1.0

# mDNS/Bonjour local network discovery (fiestaboard.local)
zeroconf>=0.131.0

# MQTT client for Home Assistant auto-discovery and control
paho-mqtt>=2.0.0

# Prometheus metrics exposition for Grafana monitoring (only loaded at runtime
# when LOCAL_MONITORING=true; kept installed so the feature works without
# rebuilding the image).
prometheus-fastapi-instrumentator>=7.1.0

# System metrics (CPU, memory, disk) for Prometheus /metrics endpoint
psutil>=6.1.0

# Optional: Board Python library (alternative to manual implementation)
# fiesta>=0.1.0

