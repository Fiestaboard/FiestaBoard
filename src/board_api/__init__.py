"""Reading from and writing to the physical board, out of band of the engine.

Named ``board_api`` because ``src/boards.py`` already owns the "which board
does this id mean" question; this package owns the HTTP surface for "what is
on the board" and "put this on the board right now".
"""
