"""Staff picks: the curated pages FiestaBoard ships with.

A pick is a page template plus the metadata the gallery card renders, served
from ``staff-picks/picks.json`` at the repo root. There is no service and no
storage — the catalog is read-only and checked in — so this package is just
the router and its models.
"""
