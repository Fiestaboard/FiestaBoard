"""Carousel module for page collection rotation.

Carousels are ordered collections of pages that cycle between one another
at a configurable interval. They can be selected as the active display
in both manual and schedule modes.
"""
