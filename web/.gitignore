# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# Vite + React Router v7 build output
/build
/.react-router
/dist

# Stale Next.js artifacts — the app migrated off Next.js to Vite + React Router v7,
# but tools occasionally regenerate these. Ignore so they don't sneak back in.
/.next
/next-env.d.ts

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# typescript
*.tsbuildinfo

# Storybook
/storybook-static

# Playwright
/test-results/
/playwright-test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/screenshot-results/
