/**
 * Seasonal theming now lives in the design system — this shim keeps the
 * historic import path working. See @fiestaboard/ui `seasons`.
 */
export { getActiveSeason, HIDE_FESTIVE_COOKIE, readCookieString, SEASONS, shouldShowPride } from "@fiestaboard/ui";
