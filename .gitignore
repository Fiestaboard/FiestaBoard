# Environment variables - NEVER commit these!
.env
.env.local
.env.*.local

# Codespaces
.devcontainer/secrets.json

# Python
__pycache__/
.coverage*
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
# Don't ignore web source lib folder
!web/src/lib/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv

# Next.js build output
web/out/
web/.next/

# IDE
.vscode/
.idea/
.cursor/
.cursorrules
*.swp
*.swo
*~

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Docker
.dockerignore
*.tar

# Config files with sensitive data
config/config.yaml
!config/config.yaml.example
config.json

# Data directory (user-specific data, contains secrets)
data/
# But track seed/default pages
!data/pages.json
# Note: data/config.json and data/settings.json contain user secrets and are NOT tracked

# External plugins (cloned at runtime from registry or git URLs)
external_plugins/

# API keys reference (optional - remove if you want to track it)
API_KEYS.md

# Docs site build artifacts
docs-site/node_modules/
docs-site/build/
docs-site/.docusaurus/
docs-site/.cache-loader/

# Local/dev screenshots (never referenced in docs - do not commit)
/audit-*.png
/verify-*.png
/mobile-menu-*.png
/menu-*-fixed.png
/menu-*-dimmed.png
