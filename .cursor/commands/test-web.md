Run tests for the web UI inside the Docker container. Execute:

docker-compose -f docker-compose.dev.yml exec fiestaboard-ui-dev npm test

Show the test results and explain any failures if they occur.




