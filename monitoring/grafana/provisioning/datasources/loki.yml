# Grafana datasource provisioning — Loki
# Loki runs in the same container on port 3101.

apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://127.0.0.1:3101
    isDefault: false
    editable: false
