{
  "_comment": "API conventions ratchet (Phase 2, spec \u00a72). Enforced by tests/test_api_conventions_ratchet.py; rules documented in docs/internal/reference/API_CONVENTIONS.md. Append a domain to converted_domains in the slice PR that converts it \u2014 never before. Every exception needs a route that exists, a known rule id, and a reason a reviewer can argue with.",
  "converted_domains": [
    "ai",
    "backup",
    "board",
    "collections",
    "config",
    "debug",
    "displays",
    "mqtt",
    "network",
    "pages",
    "panels",
    "plugin-support",
    "plugins",
    "schedules",
    "service",
    "settings",
    "staff-picks",
    "system",
    "templates",
    "transitions",
    "triggers"
  ],
  "exceptions": [
    {
      "route": "DELETE /config/board",
      "rule": "declared_errors",
      "reason": "Reset is unconditional and idempotent: no board id to miss, no state in which it refuses, and its one internal failure path is swallowed and logged on purpose so a broken settings store cannot block the wizard reset. Nothing to declare at any status."
    },
    {
      "route": "GET /",
      "rule": "declared_errors",
      "reason": "Returns three constant strings naming the API. It takes no parameter, reads no state and calls nothing, so there is no failure to declare at any status \u2014 the only 500 it could produce is an unhandled bug, which is true of every route and belongs in a log, not the schema."
    },
    {
      "route": "GET /backup/export",
      "rule": "declared_errors",
      "reason": "A parameterless dump of local state: no id to miss, no input to reject, and no dependency it calls over the network. BackupService.export_to_json reads the data dir and serializes it; a failure there is an unhandled bug, not a contract."
    },
    {
      "route": "GET /backup/export",
      "rule": "response_model",
      "reason": "Serves an opaque file download \u2014 a raw Response carrying Content-Disposition and Cache-Control: no-store, whose body is a JSON document whose shape is whatever stores existed at export time. A response_model would either be a lie (dict[str, Any]) or would make FastAPI re-serialize the body and drop the download headers."
    },
    {
      "route": "GET /collections",
      "rule": "declared_errors",
      "reason": "Listing has no failure path: it returns [] for an empty store and cannot 404 or 400, so declaring a 4xx would document an error the endpoint never raises."
    },
    {
      "route": "GET /config",
      "rule": "declared_errors",
      "reason": "No failure mode to declare at any status. It takes no path, query or body parameter and reads process-local config that always exists, so it cannot 400, cannot 404, and raises no HTTPException of its own. The rule now accepts a 5xx, but the only 500 this route can produce is an unhandled bug \u2014 true of every route, and documenting it would put noise in the schema rather than a contract."
    },
    {
      "route": "GET /config/board",
      "rule": "declared_errors",
      "reason": "Parameterless read of the deprecated legacy board view. It projects an empty boards store into defaults rather than failing, so there is no status \u2014 4xx or 5xx \u2014 that it deliberately returns."
    },
    {
      "route": "GET /config/full",
      "rule": "declared_errors",
      "reason": "Parameterless dump of the masked config document, with no HTTPException raised anywhere in the handler. Nothing the caller sends can be wrong and nothing it reads can be missing. The 5xx the widened rule would accept is only the generic unhandled-error case, which is not this route's contract."
    },
    {
      "route": "GET /config/general",
      "rule": "declared_errors",
      "reason": "Parameterless read of the general settings block, which falls back to defaults when absent. No input to validate, nothing to not-find, and no raise in the handler. Its sibling PUT does declare 422 and 500, because it really can return both."
    },
    {
      "route": "GET /config/validate",
      "rule": "declared_errors",
      "reason": "A verdict endpoint. 'This install is not configured' is the answer at 200 with valid=false, and the handler raises nothing. There is no caller mistake it rejects and no dependency it reports as unavailable."
    },
    {
      "route": "GET /debug/network-diagnostics",
      "rule": "declared_errors",
      "reason": "The whole point of the endpoint is to report connectivity failures in its 200 body; the caller supplies no input to get wrong. The only failure is the diagnostics runner itself blowing up, which is a 500."
    },
    {
      "route": "GET /debug/system-info",
      "rule": "declared_errors",
      "reason": "Has no client-error path at all: every collaborator it reads (connection info, uptime, cache status, board-configured probe) is defensive and answers with a fallback rather than raising, so the card renders for an unconfigured install too. Declaring a 4xx would document a failure this route cannot produce."
    },
    {
      "route": "GET /displays",
      "rule": "declared_errors",
      "reason": "No failure path: an instance with no plugins installed answers an empty list with total 0. It reads the in-process plugin registry, which always exists, and reports availability per entry rather than failing on an unavailable one."
    },
    {
      "route": "GET /health",
      "rule": "declared_errors",
      "reason": "The liveness probe nginx, Docker's HEALTHCHECK and the web boot gate all poll. It answers 200 whenever the process is up and reports the display loop's state in the body; a health check that can 4xx is a health check that takes the container down for a stopped display loop, which is the opposite of what it is for."
    },
    {
      "route": "GET /network/wifi/capability",
      "rule": "declared_errors",
      "reason": "This is the probe every other /network/wifi route 501s against, and it must answer 200 even \u2014 especially \u2014 when WiFi is unmanageable: {available: false, reason} is the payload the UI hides the Network tab on. A refusal here would leave the client unable to distinguish 'no WiFi support' from 'this build has no such endpoint'."
    },
    {
      "route": "GET /pages",
      "rule": "declared_errors",
      "reason": "Has no failure path of its own: an instance with no pages answers {\"pages\": [], \"total\": 0}. The only 4xx it can produce is the auth middleware's 401, which is applied app-wide and is not this route's contract to declare."
    },
    {
      "route": "GET /pages/cache/stats",
      "rule": "declared_errors",
      "reason": "Reports in-memory preview-cache occupancy, which always exists \u2014 an empty cache is a valid answer, not an error. Nothing it reads can be missing or unauthorized at the route level."
    },
    {
      "route": "GET /panels",
      "rule": "declared_errors",
      "reason": "No failure path: an instance with no panels answers an empty list with total 0. It reads panel storage plus the boards list; a panel whose board is gone is reported inline as board_missing rather than failing the request."
    },
    {
      "route": "GET /plugins/errors",
      "rule": "declared_errors",
      "reason": "The endpoint whose job is to report failures cannot itself fail: with the plugin system down it answers plugin_system_enabled=false and empty maps, which is the correct answer, not an error."
    },
    {
      "route": "GET /plugins/variables/all",
      "rule": "declared_errors",
      "reason": "Has no failure path at all: with the plugin system down it falls back to the template engine's own variables rather than erroring, because the template editor still has a vocabulary without plugins."
    },
    {
      "route": "GET /schedules",
      "rule": "declared_errors",
      "reason": "No failure path. The only argument is board_id, and a board-scoped read deliberately falls back to the empty list instead of 404ing (API_CONVENTIONS.md, 'board_id validation: writes 404, reads fall back'); board_id=* is a documented wildcard, not an id."
    },
    {
      "route": "GET /schedules/active/page",
      "rule": "declared_errors",
      "reason": "No failure path. It reports whatever is on the board right now \u2014 manual page, scheduled page, or nothing \u2014 and every collection resolution failure is caught and reported as null rather than raised, because a dashboard poll must not error out over a collection that cannot resolve."
    },
    {
      "route": "GET /schedules/default-page",
      "rule": "declared_errors",
      "reason": "No failure path. An unknown board_id deliberately answers the global default rather than 404 \u2014 the same read/write asymmetry, decided in #1888 and documented in API_CONVENTIONS.md."
    },
    {
      "route": "GET /schedules/enabled",
      "rule": "declared_errors",
      "reason": "No failure path. An unknown board_id deliberately answers false rather than 404 \u2014 the same read/write asymmetry, decided in #1888 and documented in API_CONVENTIONS.md."
    },
    {
      "route": "GET /silence-status",
      "rule": "declared_errors",
      "reason": "A read the dashboard polls on a timer. An unknown board_id falls back to the safe answer rather than 404ing, which API_CONVENTIONS.md \u00a7board_id validation makes the rule for reads precisely so a tab polling a board another tab just deleted does not raise an error toast. Nothing else it reads can be absent."
    },
    {
      "route": "GET /staff-picks",
      "rule": "declared_errors",
      "reason": "Reads the bundled staff-picks/picks.json and degrades a missing file to [] on purpose, so a caller can never see a 4xx from it. The per-pick 404 lives on GET /staff-picks/{pick_id}/share, which declares it."
    },
    {
      "route": "GET /system/update-check",
      "rule": "declared_errors",
      "reason": "Probe endpoint (API_CONVENTIONS.md, \"Status codes\"): an unreachable Docker Hub or GitHub is a verdict about something else, reported at 200 in the declared UpdateCheckResponse.error field. Nothing the caller sends can make this route 4xx."
    },
    {
      "route": "GET /system/update/status",
      "rule": "declared_errors",
      "reason": "Read-only report with no failure path: it cannot 404, 400 or 409, so declaring a 4xx would document an error the endpoint never raises. An unreachable sidecar is reported as updater_available=false, not as an error."
    },
    {
      "route": "GET /templates/formula-functions",
      "rule": "declared_errors",
      "reason": "No failure path: the function table is a module-level constant in src/templates/expressions.py with no input and no I/O, so nothing the caller controls can make it fail."
    },
    {
      "route": "GET /templates/variables",
      "rule": "declared_errors",
      "reason": "No failure path: it reads the in-process template engine and plugin registry, both of which always exist. An install with no plugins answers empty catalogs alongside the static colour/symbol/filter tables \u2014 an empty answer, not an error."
    },
    {
      "route": "GET /triggers",
      "rule": "declared_errors",
      "reason": "No failure path: an instance with nothing fired answers an empty list with count 0. It reads in-process trigger state that always exists."
    },
    {
      "route": "GET /triggers/active",
      "rule": "declared_errors",
      "reason": "No failure path by design: the dashboard polls this on a timer, so \"nothing is firing\" is an explicit null trigger at 200 rather than a 404 the client would have to treat as normal."
    },
    {
      "route": "GET /version",
      "rule": "declared_errors",
      "reason": "Read-only report with no failure path: it cannot 404, 400 or 409, so declaring a 4xx would document an error the endpoint never raises. It reads two env vars and one /proc file, and reports None when the file is absent."
    },
    {
      "route": "HEAD /health",
      "rule": "declared_errors",
      "reason": "The same handler as GET /health, split into its own operation only so FastAPI emits a distinct operationId (#1572). It cannot fail differently from the route it delegates to."
    },
    {
      "route": "POST /config/board/enable-local-api",
      "rule": "no_200_on_failure",
      "reason": "Same declared verdict contract as POST /config/board/test (#1887): 'that enablement token is not valid' is the board's answer at 200 through EnableLocalApiResponse. The SSRF and host-validation rejections stayed 400 and the unexpected-exception path stayed 500 when Task 10a fixed this handler."
    },
    {
      "route": "POST /config/board/test",
      "rule": "no_200_on_failure",
      "reason": "Declared probe contract (#1887, Task 10a): the endpoint's job is to report what the board said, so 'your key was rejected' is the requested answer at 200 through BoardTestResponse, not a transport failure. Preconditions the server rejects before probing (missing credential, unsafe host) really are 400, and unanticipated errors really are 500 \u2014 tests/test_status_code_correctness.py pins both. Making the upstream verdicts 4xx would tell the wizard the request was malformed when it was not."
    },
    {
      "route": "POST /pages/ai/chat",
      "rule": "response_model",
      "reason": "Serves a Server-Sent Events stream, not a JSON document: the body is a sequence of `event:`/`data:` frames written over an open connection, and any response_model would either be a lie about what the client receives or would make FastAPI buffer and re-serialize the stream, which is the one thing an SSE endpoint must not do. The schema is published instead as the 200's text/event-stream content type plus CHAT_STREAM_EVENTS in src/ai/page_routes.py, which names each of the five event types and the model describing its payload; tests/test_ai_pages_contract.py validates real frames against that registry and cross-checks it against the literal event dicts in src/ai/chat.py, so it cannot drift the way an undocumented stream would."
    },
    {
      "route": "POST /plugins/{plugin_id}/options/{options_id}",
      "rule": "no_200_on_failure",
      "reason": "Three returns inside except blocks, all deliberate. Two serve the last good options with stale=true when the provider times out or throws, so a settings form mid-edit keeps its picker instead of blanking. The third turns OptionsUnavailable (\"no API key yet\") into a 200 with an error hint, because that is the expected state while the user is still filling in the very field the credential goes in \u2014 a 4xx there would surface a failed-request toast for a form that is simply incomplete."
    },
    {
      "route": "POST /schedules/validate",
      "rule": "declared_errors",
      "reason": "No failure path. It is a read of existing schedules with an optional board_id, which falls back to the empty set; an inconsistent schedule set is the 200 verdict this endpoint exists to report, not an error."
    },
    {
      "route": "POST /stop",
      "rule": "declared_errors",
      "reason": "Idempotent with no failure path: stopping an already-stopped loop is the state the caller asked for, so it answers 200 with changed=false. It touches no resource that can be missing and validates no input \u2014 there is nothing it can refuse."
    },
    {
      "route": "POST /triggers/clear",
      "rule": "declared_errors",
      "reason": "No failure path: clearing an empty trigger set is a successful no-op, and there is no id or body that could be wrong."
    },
    {
      "route": "GET /settings/mqtt",
      "rule": "declared_errors",
      "reason": "Read-only, no path or body parameters: it returns the stored MQTT block and has no failure path to declare. Adding an error status would document a failure this endpoint cannot produce."
    },
    {
      "route": "GET /settings/ai",
      "rule": "declared_errors",
      "reason": "Read-only, no path or body parameters: it returns the stored AI provider block and has no failure path to declare. Adding an error status would document a failure this endpoint cannot produce."
    },
    {
      "route": "GET /settings/transitions",
      "rule": "declared_errors",
      "reason": "Read-only, no path or body parameters: it returns the stored transition settings and has no failure path to declare. Adding an error status would document a failure this endpoint cannot produce."
    },
    {
      "route": "GET /settings/output",
      "rule": "declared_errors",
      "reason": "Read-only, no path or body parameters: it returns the stored output target and has no failure path to declare. Adding an error status would document a failure this endpoint cannot produce."
    },
    {
      "route": "GET /settings/polling",
      "rule": "declared_errors",
      "reason": "Read-only, no path or body parameters: it returns the stored polling intervals and has no failure path to declare. Adding an error status would document a failure this endpoint cannot produce."
    },
    {
      "route": "GET /settings/board",
      "rule": "declared_errors",
      "reason": "Read-only, no path or body parameters: it returns the stored board settings and has no failure path to declare. Adding an error status would document a failure this endpoint cannot produce."
    },
    {
      "route": "GET /settings/display",
      "rule": "declared_errors",
      "reason": "Read-only, no path or body parameters: it returns the stored web-UI display preferences and has no failure path to declare. Adding an error status would document a failure this endpoint cannot produce."
    },
    {
      "route": "GET /settings/location",
      "rule": "declared_errors",
      "reason": "Read-only, no path or body parameters: it returns the stored location coordinates and has no failure path to declare. Adding an error status would document a failure this endpoint cannot produce."
    },
    {
      "route": "GET /settings/plugins",
      "rule": "declared_errors",
      "reason": "Read-only, no path or body parameters: it returns the stored plugin-system settings and has no failure path to declare. Adding an error status would document a failure this endpoint cannot produce."
    },
    {
      "route": "GET /settings/beta",
      "rule": "declared_errors",
      "reason": "Read-only, no path or body parameters: it returns the stored beta flags plus their runtime status and has no failure path to declare. Adding an error status would document a failure this endpoint cannot produce."
    },
    {
      "route": "GET /settings/all",
      "rule": "declared_errors",
      "reason": "Read-only, no path or body parameters: it returns the stored the whole settings page in one payload and has no failure path to declare. Adding an error status would document a failure this endpoint cannot produce."
    },
    {
      "route": "GET /settings/active-page",
      "rule": "declared_errors",
      "reason": "Takes an optional board_id and deliberately falls back rather than 404ing on an unknown one \u2014 the board-scoped-read rule in API_CONVENTIONS.md ('reads fall back', decided with #1888). With the fallback in place there is no error status left to declare, and adding one would document a failure this endpoint is specifically designed not to raise."
    },
    {
      "route": "GET /settings/temporary-override",
      "rule": "declared_errors",
      "reason": "Takes no input and always answers: the active override, or every field nulled when there is none. 'No override' is a 200, not a 404 \u2014 the absence is the answer the dashboard polls for."
    },
    {
      "route": "DELETE /settings/temporary-override",
      "rule": "declared_errors",
      "reason": "Idempotent cancel with no path or body parameters. Clearing when nothing is active is a success, not a 404 \u2014 the caller's intent (no override) is satisfied either way, and the dashboard's cancel button races the override's own expiry."
    },
    {
      "route": "GET /settings/hdmi-kiosk",
      "rule": "declared_errors",
      "reason": "A status read with no input. Every failure mode it has is reported in the 200 body \u2014 'unsupported' off a FiestaPi, 'unknown' when the sidecar is unreachable \u2014 because the caller is asking about the platform, not addressing a resource that can be missing."
    },
    {
      "route": "POST /settings/hdmi-kiosk",
      "rule": "no_200_on_failure",
      "reason": "The flagged return is the `except` around `resp.json()`, reached only after the sidecar has already answered 200 or 202 \u2014 the enable was accepted and the work is queued. Returning {'status': 'queued'} there is correct; the sidecar simply sent no JSON body. Every real failure above it is already a 4xx/502. Called out as do-not-touch in the Phase 2 plan's silent-failure enumeration (Task 10a)."
    }
  ]
}
